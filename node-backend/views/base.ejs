<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Inventory Management System' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/style.css">
    <style>
        * { font-size: 12px; }
        body { font-family: 'Inter', sans-serif; background-color: #f4f6f7; }
        .sidebar { position: fixed; top: 0; bottom: 0; left: 0; z-index: 100; padding: 12px 0 0; box-shadow: inset -1px 0 0 rgba(0, 0, 0, .1); background-color: #2c3e50; width: 250px; }
        .sidebar-sticky { position: relative; top: 0; height: calc(100vh - 48px); padding-top: .5rem; overflow-x: hidden; overflow-y: auto; display: flex; }
    </style>
</head>
<body>
    <!-- Page content will be injected here by child templates after including base.ejs -->
</body>
</html>
